import {ADD_MILESTONE} from '../../actions/user/index'

const tagCategories = (state = [], action) => {
	switch (action.type){
		case: ADD_MILESTONE
			return state
		default:
			return state
	}
}

export default tagCategories